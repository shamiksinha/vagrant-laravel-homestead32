@extends('layouts.app') 

@section('content')
<p>Kind of a wash between this technique and the previous one, as they
	both require extra Kind of a wash between this technique and the
	previous one, as they both require extraKind of a wash between this
	technique and the previous one, as they both require extraKind of a
	wash between this technique and the previous one, as they both require
	extraKind of a wash between this technique and the previous one, as
	they both require extraKind of a wash between this technique and the
	previous one, as they both require extraKind of a wash between this
	technique and the previous one, as they both require extraKind of a
	wash between this technique and the previous one, as they both require
	extraKind of a washbetween this technique and the previous one, as they
	both require extraKind of a wbetween this technique and the previous
	one, as they both require extra technique and the previous one, as they
	both require extraKind of a wash between this technique and the
	previous one, as they both require extraKind of a wash between this
	technique and the previous one, as they both require extraKind of a
	wash between this technique and the previous one, as they both require
	extraKind of a wash between this technique and the previous one, as
	they both require extraKind of a wash between this</p>
@endsection
